import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tjmorris.adhdwomenshealth',
  appName: "ADHD Women's Health",
  webDir: 'dist'
};

export default config;
